import { AgendaFilterPipe } from './agenda-filter.pipe';

describe('AgendaFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AgendaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
